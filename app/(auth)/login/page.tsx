import { login } from "./actions";
import { Card, Button } from "@/components/ui";
export default function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { void searchParams; return <main className="mx-auto flex min-h-screen max-w-md items-center px-4"><Card className="w-full"><h1 className="mb-2 text-2xl font-bold">KlimaGuy Login</h1><p className="mb-6 text-slate-600">Interner Zugang ohne öffentliche Registrierung.</p><form action={login} className="space-y-4"><input name="email" type="email" required placeholder="E-Mail" className="w-full rounded border p-3"/><input name="password" type="password" required placeholder="Passwort" className="w-full rounded border p-3"/><Button type="submit" className="w-full">Einloggen</Button></form></Card></main>; }
