import fs from "node:fs";import path from "node:path";import {describe,expect,it} from "vitest";
const sql=fs.readFileSync(path.join(process.cwd(),"supabase/migrations/202609180002_klimaguy_knowledge_learning.sql"),"utf8");
describe("knowledge migration",()=>{it("creates two global RLS authorities and narrow RPCs",()=>{expect(sql).toContain("create table public.klimaguy_knowledge_entries");expect(sql).toContain("create table public.klimaguy_learning_candidates");expect(sql.match(/enable row level security/g)).toHaveLength(2);expect(sql).toContain("record_klimaguy_learning_candidates");expect(sql).toContain("review_klimaguy_learning_candidate");expect(sql).toContain("auth.role()<>'service_role'");expect(sql).toContain("jsonb_array_length(target_candidates)>2");expect(sql).toContain("on conflict (fingerprint) where status='pending'")});it("does not repurpose project evidence authorities",()=>{expect(sql).not.toMatch(/project_knowledge_states|project_knowledge_claims|evidence_claim_proposals/)})});
